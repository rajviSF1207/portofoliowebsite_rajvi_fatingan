services:
  server:
    container_name: ubuntu-rajvifatingan
    build:
      context: .
      dockerfile: Dockerfile
    image: rajvi04sf/portofolioweb_rajvi_fatingan:latest
    ports:
      - "8080:80"
    restart: unless-stopped